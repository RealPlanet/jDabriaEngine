package engine.ecp.interfaces;

/**
 * If a component implements this interface addComponent will throw an exception if multiple of such component are found.
 * IE - "THERE CAN ONLY BE ONE" - The Highlander
 */
public interface SingleComponent {
}
